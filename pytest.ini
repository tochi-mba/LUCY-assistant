[pytest]
# Tests for the meta repository's own scripts. Run with:
#   uv run --with pytest pytest tests -q
# (add --with pyyaml to include the docker-compose.yml contract tests)
testpaths = tests
addopts = -ra --strict-markers --strict-config
filterwarnings =
    error
